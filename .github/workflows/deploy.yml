name: Deploy to Production

on:
  push:
    branches:
      - main

# Only one deployment runs at a time — cancels queued deploys if a new push
# comes in before the previous one finishes.
concurrency:
  group: production-deploy
  cancel-in-progress: false

jobs:
  # CI must pass before deploy even starts
  backend-ci:
    uses: ./.github/workflows/backend-ci.yml

  # deploy-backend:
  #   name: Deploy Backend
  #   runs-on: ubuntu-latest
  #   needs: backend-ci

  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Deploy to VPS via SSH
  #       uses: appleboy/ssh-action@v1.0.3
  #       with:
  #         host: ${{ secrets.VPS_HOST }}
  #         username: ${{ secrets.VPS_USER }}
  #         key: ${{ secrets.VPS_SSH_KEY }}
  #         port: 22
  #         script: |
  #           set -e
  #           cd ${{ secrets.VPS_APP_PATH }}

  #           echo "── Pulling latest code ──"
  #           git pull origin main

  #           echo "── Rebuilding production images ──"
  #           docker compose -f docker-compose.production.yml build --no-cache

  #           echo "── Running database migrations ──"
  #           docker compose -f docker-compose.production.yml run --rm app alembic upgrade head

  #           echo "── Restarting services ──"
  #           docker compose -f docker-compose.production.yml up -d --remove-orphans

  #           echo "── Waiting for app to be healthy ──"
  #           sleep 10

  #           echo "── Health check ──"
  #           curl --fail --silent --max-time 10 http://localhost:8000/health \
  #             && echo "Health check passed" \
  #             || (echo "Health check FAILED" && exit 1)

  deploy-frontend:
    name: Deploy Frontend
    runs-on: ubuntu-latest
    needs: backend-ci
    # Vercel auto-deploys on push to main — this job just verifies it succeeded.
    steps:
      - name: Wait for Vercel deployment
        run: |
          echo "Frontend deployment is handled automatically by Vercel."
          echo "Check https://vercel.com/dashboard for deployment status."
